let person = {
  name: "Adnan",
  age: 25,
  country: "Bangladesh"
}

function logData() {
  let intro =
    person.name + " is " + person.age + " years old and lives in " + person.country;
  console.log(intro);
}

logData();
